import colorAdjust from 'nanocloud/utils/color-adjust';
import { module, test } from 'qunit';

module('Unit | Utility | color adjust');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = colorAdjust();
  assert.ok(result);
});
