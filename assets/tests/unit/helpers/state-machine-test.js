import { stateMachine } from 'nanocloud/helpers/state-machine';
import { module, test } from 'qunit';

module('Unit | Helper | state machine');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stateMachine([42]);
  assert.ok(result);
});
