import { reverse } from 'nanocloud/helpers/reverse';
import { module, test } from 'qunit';

module('Unit | Helper | reverse');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reverse([42]);
  assert.ok(result);
});
