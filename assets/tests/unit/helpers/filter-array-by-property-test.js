import { filterArrayByProperty } from 'nanocloud/helpers/filter-array-by-property';
import { module, test } from 'qunit';

module('Unit | Helper | filter array by property');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterArrayByProperty([42]);
  assert.ok(result);
});
