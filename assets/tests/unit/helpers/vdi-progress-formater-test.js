import { vdiProgressFormater } from 'nanocloud/helpers/vdi-progress-formater';
import { module, test } from 'qunit';

module('Unit | Helper | vdi progress formater');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vdiProgressFormater([42]);
  assert.ok(result);
});
