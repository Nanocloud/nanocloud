engines:
  eslint:
    enabled: true
    checks:
      comma-dangle:
        enabled: false
    enabled: true
    config:
      languages:
      - javascript
ratings:
  paths:
  - "**.js"
exclude_paths:
  - tests/api/**
  - assets/tests/**
